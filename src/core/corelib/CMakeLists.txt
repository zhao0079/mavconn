SET_SOURCE_FILES(CORELIB_SRC_FILES
  Core.cc
  Exception.cc
  SignalHandler.cc
  Sleep.cc
  SGThread.cxx
)

IF (UNIX)
  ADD_SOURCE_FILES(CORELIB_SRC_FILES timer/OgreTimer.Unix.cpp Clock.cc)
ELSEIF (WIN32)
  ADD_SOURCE_FILES(CORELIB_SRC_FILES timer/OgreTimer.WIN32.cpp)
ENDIF ()

PIXHAWK_LIBRARY(corelib SHARED ${CORELIB_SRC_FILES})
PIXHAWK_LINK_LIBRARIES(corelib
  ${Boost_SYSTEM_LIBRARY}
  ${Boost_FILESYSTEM_LIBRARY}
  ${CMAKE_THREAD_LIBS_INIT}
)

