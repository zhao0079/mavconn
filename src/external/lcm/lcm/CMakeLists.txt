INCLUDE_DIRECTORIES(
  ${GLIB2_MAIN_INCLUDE_DIR}
  ${GLIB2_INTERNAL_INCLUDE_DIR}
  ${GTHREAD2_MAIN_INCLUDE_DIR}
)

SET_SOURCE_FILES(LCM_SRC_FILES  eventlog.c  lcm_file.c  lcm_udp.c  lcm.c  ringbuffer.c
)
PIXHAWK_LIBRARY(lcm SHARED ${LCM_SRC_FILES})
SET_TARGET_PROPERTIES(lcm PROPERTIES COMPILE_FLAGS -D_REENTRANT)
#SET_TARGET_PROPERTIES(lcm PROPERTIES COMPILE_FLAGS -std=c99)
PIXHAWK_LINK_LIBRARIES(lcm
  ${GLIB2_LIBRARY}
  ${GTHREAD2_LIBRARY}
)

